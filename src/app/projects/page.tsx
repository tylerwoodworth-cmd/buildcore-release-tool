import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      sub="Active release projects"
      next="Project card grid + per-project hub (Overview, Lifecycle, Tickets, Feedback, Testing, Pending) lands in a follow-up commit."
    />
  );
}
