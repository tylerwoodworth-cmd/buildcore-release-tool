import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function RoadmapPage() {
  return (
    <PagePlaceholder
      title="BuildCore roadmap"
      sub="2026 release plans across all projects — phase + feature view"
      next="Cross-project Gantt with expandable feature rows lands in a follow-up commit."
    />
  );
}
