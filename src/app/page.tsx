import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Welcome back, Tyler"
      sub="Snapshot across all BuildCore release projects"
      next="Dashboard widgets (My tasks, KPIs, recent activity, ticket pipeline) will land in the next commit."
    />
  );
}
