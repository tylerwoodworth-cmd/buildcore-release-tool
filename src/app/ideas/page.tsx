import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function IdeasPage() {
  return (
    <PagePlaceholder
      title="Ideas"
      sub="New projects, new features, and enhancements — three lanes so smaller ideas stay visible"
      next="Three-tab capture interface (New projects / New features / Enhancements) lands in a follow-up commit."
    />
  );
}
